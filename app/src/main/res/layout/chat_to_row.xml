<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/to_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@drawable/messages"
        android:maxWidth="240dp"
        android:padding="16dp"
        android:text="from user"
        app:layout_constraintEnd_toStartOf="@+id/to_imageView"
        app:layout_constraintTop_toTopOf="@+id/to_imageView" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/to_imageView"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/rounded_img_selector" />
</androidx.constraintlayout.widget.ConstraintLayout>